import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StatingPage } from './stating';

@NgModule({
  declarations: [
    StatingPage,
  ],
  imports: [
    IonicPageModule.forChild(StatingPage),
  ],
})
export class StatingPageModule {}
